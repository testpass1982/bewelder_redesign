---
- name: Installing apt packages
  apt:
    name: nginx
    state: latest
  tags: nginx
  notify:
  - restart nginx

- name: Nginx configuration
  template:
    src: default
    dest: /etc/nginx/sites-available/default
  tags: nginx
  notify:
  - restart nginx
